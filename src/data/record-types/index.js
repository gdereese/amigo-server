const types = {
  friend: require('./friend'),
  friendRequest: require('./friend-request')
};

const typeNames = {
  friend: 'friend',
  friendRequest: 'friendRequest'
};

module.exports = {
  types,
  typeNames
};
